# ============================================================================
# 海上风电智能体项目依赖 (Offshore Wind AI Agent)
# ============================================================================
# 安装方式:
#   pip install -r requirements.txt
#
# 注意: torch 需要根据你的 CUDA 版本选择对应的 index, 见下方说明
# ============================================================================

# ---- 核心依赖 (必须) ----
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
flask>=3.0.0
joblib>=1.3.0

# ---- 深度学习 / DQN (必须, 需要 GPU 版 PyTorch) ----
# Windows 上 pip install torch 默认装 CPU 版本, 训练会很慢
# 请根据 nvidia-smi 显示的 CUDA 版本选择:
#
#   CUDA 12.1:  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8:  pip install torch --index-url https://download.pytorch.org/whl/cu118
#   CPU only:   pip install torch (不推荐, DQN 训练会很慢)
#
# 下面这行不指定 index, 会装 CPU 版本; 建议手动执行上面命令替换:
torch>=2.1.0
tensorboard>=2.14.0

# ---- 可选: LightGBM 预测模型对比 ----
# 未安装时自动回退到 sklearn HistGBR, 不影响运行
lightgbm>=4.0.0
